import { HeroCanvas } from "@/app/components/HeroCanvas";

/** One full-viewport atmosphere canvas. Do not also mount HeroCanvas in pages. */
export function SiteAtmosphere() {
  return <HeroCanvas />;
}
